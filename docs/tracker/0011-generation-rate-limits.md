---
id: "0011"
title: 生成限流
labels: [done]
status: done
---

# 11 — 生成限流

**What to build:** 给真实 API 额度装上护栏：每用户每小时生成次数上限 + 全局同时进行的生成并发上限；超限返回 429 与预计可重试时间，前端给出友好提示而非报错。限额参数经环境变量可调。

**Blocked by:** 03（工程师模式生成主链路）

**Status:** done

- [x] 超出每用户每小时上限时生成请求返回 429，响应携带重试等待信息
- [x] 全局并发超限时新生成被拒绝（或排队策略明确），不影响进行中的生成
- [x] 前端收到限流响应时展示友好提示（何时可再试），而非通用错误
- [x] 限额参数经环境变量配置，测试以可控时钟验证边界行为
